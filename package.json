{
  "name": "typescriptc",
  "version": "0.1.0",
  "description": "TypeScript to C Compiler with Rust-style memory management",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "tsc": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
